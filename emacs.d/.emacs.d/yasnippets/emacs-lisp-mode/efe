# name: entete elisp
# key: efe
# --
;;; `(file-name-nondirectory (buffer-file-name))` --- ${1:Description} -*- lexical-binding: t; -*-

;;; Commentary:

;; $2

;;; Code:

$0

(provide '${3:`(file-name-base (file-name-nondirectory (buffer-file-name)))`})

;;; `(file-name-nondirectory (buffer-file-name))` ends here
